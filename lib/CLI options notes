BuildpathAction defines the following option:
  - buildpath

BuildtreeAction (extends BuildpathAction) defines the following option on the ACTION instance options:
  - buildtree (getBuildtreeFromArgs)

CommandRunnerAction (extends BuildtreeAction) defines the following options:
  - disableCache
  - maxCacheSize
  - workspace